"""
LSTM model for solar and wind generation forecasting.

Phase 3: Sequence-to-sequence deep learning with MC Dropout
for uncertainty estimation.
"""


def build_lstm(input_window: int, n_features: int, hidden_units: int = 64):
    """Build LSTM architecture with dropout layers."""
    raise NotImplementedError("Phase 3 — LSTM model planned")


def mc_dropout_predict(model, X, n_samples: int = 100):
    """Monte Carlo Dropout inference for uncertainty estimation."""
    raise NotImplementedError("Phase 3 — LSTM model planned")
